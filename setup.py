from setuptools import setup, find_packages

setup(
    name='discomplex',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # list your package dependencies here
    ],
)


